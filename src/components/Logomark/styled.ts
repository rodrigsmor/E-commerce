import styled from 'styled-components';

export const Logomark = styled.div`
    display: flex;
    color: #962062;
    font-size: 20px;
    cursor: pointer;
    align-items: center;

    h1 {
        font-size: 20px;
        font-weight: 500;
        margin: 0 0 0 5px;
    }

    i, span {
        color: #fe4365;
    }
`;